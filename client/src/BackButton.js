var React = require('react');

const BackButton = ({viewOrders}) => {
  return (
    <div className='center'>
      <button className='red-button new-group' onClick={() => viewOrders()}><a href='#'>Go Baáâäæãāåàck!!!!!!!!!!!!!!!</a></button>
    </div>
  );
};

module.exports = BackButton;